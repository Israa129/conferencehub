// src/environments/environment.ts
// Vérifie que ce fichier existe dans ton projet.
// Si il est déjà créé par un autre membre, assure-toi juste que apiUrl est bien défini.

export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api'  // adapte le port si nécessaire
};